// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { Asset } from "./asset_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `asset`.
 *
 * Obtain a handle from the [`asset`] property on [`RemoteTables`],
 * like `ctx.db.asset`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.asset.on_insert(...)`.
 */
export class AssetTableHandle {
  tableCache: TableCache<Asset>;

  constructor(tableCache: TableCache<Asset>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<Asset> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `asset_id` unique index on the table `asset`,
   * which allows point queries on the field of the same name
   * via the [`AssetAssetIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.asset.asset_id().find(...)`.
   *
   * Get a handle on the `asset_id` unique index on the table `asset`.
   */
  asset_id = {
    // Find the subscribed row whose `asset_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): Asset | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.asset_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: Asset) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: Asset) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: Asset) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: Asset) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: Asset, newRow: Asset) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: Asset, newRow: Asset) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
